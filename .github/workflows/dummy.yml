name: Dummy Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  dummy-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a dummy script
        run: |
          echo "This is a dummy job to generate events."
          echo "Run ID: $GITHUB_RUN_ID"
